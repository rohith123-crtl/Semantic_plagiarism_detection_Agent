# Core (already available in most environments)
numpy>=1.24
torch>=2.0

# Recommended for full semantic power (install when network allows)
# sentence-transformers>=2.2.0
# scikit-learn>=1.3
# streamlit>=1.28   # optional nice UI

# Optional for PDF and DOCX support
pypdf>=3.0
python-docx>=1.0
